{
  "name": "putnam-journal",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "A full-stack Putnam problem journal on Cloudflare Workers, D1, R2, and Email Service.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs && node --test shared/*.test.js && npm run build",
    "dev": "npx wrangler dev --test-scheduled",
    "deploy": "npx wrangler deploy",
    "db:migrate:local": "npx wrangler d1 migrations apply putnam-journal --local",
    "db:migrate:remote": "npx wrangler d1 migrations apply putnam-journal --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
